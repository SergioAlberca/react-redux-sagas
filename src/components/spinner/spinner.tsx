import React from "react";
import { StyledSpinner } from "../styled-common-components/styled-common-components";

function Spinner() {
  return <StyledSpinner color="secondary" />;
}

export default Spinner;
